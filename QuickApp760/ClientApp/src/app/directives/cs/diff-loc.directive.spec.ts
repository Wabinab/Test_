import { DiffLocDirective } from './diff-loc.directive';

describe('DiffLocDirective', () => {
  it('should create an instance', () => {
    const directive = new DiffLocDirective();
    expect(directive).toBeTruthy();
  });
});
